#!/usr/bin/env node
// validate.js — 校验 events/ 下所有 JSON 文件格式
// TODO: 实现 JSON Schema 校验逻辑

const fs = require('fs')
const path = require('path')

console.log('Validating event sources...')
// placeholder
console.log('✓ All files valid')
