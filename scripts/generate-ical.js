#!/usr/bin/env node
// generate-ical.js — 读取 events/ 下所有 JSON，生成对应 .ics 文件到 ical/
// TODO: 使用 ical-generator 库实现

const fs = require('fs')
const path = require('path')

console.log('Generating .ics files...')
// placeholder
console.log('✓ Done')
