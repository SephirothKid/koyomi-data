name: Generate iCal

on:
  push:
    branches: [main]
    paths:
      - 'events/**'

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: node scripts/generate-ical.js
      - name: Commit generated .ics files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ical/
          git diff --staged --quiet || git commit -m "chore: regenerate .ics files"
          git push
